"use strict";
const userName = "Cat";
console.log(typeof userName);
const settings = {
    difficulty: "easy",
    minLevel: 10,
    didStart: false,
    players: ["John", "Jane"],
};
const loadData = (settings) => { };
loadData(settings);
