"use strict";
let mathOperations = {
    add(a, b) {
        return a + b;
    },
    substract(a, b) {
        return a - b;
    },
};
let mathResults = {
    add: mathOperations.add(1, 2),
    substract: mathOperations.substract(5, 4),
};
